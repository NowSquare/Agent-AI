# ------------------------------------------------------------------------------
# Agent-AI — Local (non-Docker) Development (macOS with Herd)
# ------------------------------------------------------------------------------
# Note for Windows/Linux devs: prefer the Docker setup (.env.docker). You CAN run
# a non-Docker stack on Windows/Linux (PHP/Postgres/Redis/Mailpit installed locally),
# but we recommend Docker for simplicity and parity.
# ------------------------------------------------------------------------------

# Start Mailpit separately (brew or docker), it must be running for inbound email

APP_NAME="Agent AI"
APP_ENV=local
APP_DEBUG=true
APP_URL="http://agent-ai.test"
APP_TIMEZONE="Europe/Amsterdam"

# ------------------------------------------------------------------------------
# Database (PostgreSQL via Herd)
# ------------------------------------------------------------------------------
DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=agent_ai
DB_USERNAME=root
DB_PASSWORD=

# ------------------------------------------------------------------------------
# Cache / Queues / Sessions (Redis on host)
# ------------------------------------------------------------------------------
CACHE_DRIVER=redis
SESSION_DRIVER=redis
QUEUE_CONNECTION=redis
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# ------------------------------------------------------------------------------
# Outbound Mail (DEV = Mailpit; UI: http://localhost:8025)
# ------------------------------------------------------------------------------
# MAIL_FROM_ADDRESS is the *sender identity* used by Laravel.
MAIL_MAILER=smtp
MAIL_HOST=127.0.0.1
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="noreply@agent-ai.test"
MAIL_FROM_NAME="Agent AI"

# ------------------------------------------------------------------------------
# Inbound Mail (DEV receive path = Mailpit → webhook to Laravel)
# ------------------------------------------------------------------------------
# AGENT_MAIL is the *public-facing* address shown to users and docs.
# In dev, you SEND to this address *through* Mailpit's SMTP (127.0.0.1:1025).
# Mailpit then POSTs the message to our Laravel webhook with a shared secret.
AGENT_MAIL="hello@agent-ai.test"

INBOUND_EMAIL_DRIVER="mailpit"               # 'mailpit' | 'postmark'
INBOUND_WEBHOOK_SECRET="dev_shared_secret"   # change to a random string locally

# ------------------------------------------------------------------------------
# LLM (local Ollama)
# ------------------------------------------------------------------------------
LLM_PROVIDER=ollama
LLM_MODEL="gpt-oss:20b"
OLLAMA_BASE_URL="http://localhost:11434"

# ------------------------------------------------------------------------------
# Antivirus (ClamAV on host)
# ------------------------------------------------------------------------------
CLAMAV_HOST=127.0.0.1
CLAMAV_PORT=3310

# ------------------------------------------------------------------------------
# Filesystem
# ------------------------------------------------------------------------------
FILESYSTEM_DISK=local
